import React from 'react';
import { Typography, Box } from '@mui/material';

const Home: React.FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom>MCP</Typography>
    <Typography variant="body1">Master Control Program — build and test orchestration.</Typography>
  </Box>
);

export default Home;
