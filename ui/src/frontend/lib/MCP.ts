// Automatically generated by cinp-codegen from http://localhost:8899/api/v1/ at 2026-03-22T04:04:10.497623+00:00

import CInP, { List } from 'cinp'

export interface ListFilter {};
export interface QueryFilter { toFilter(): object | undefined };
export class ListFilterAll implements ListFilter {};
type ModelUpdate<T> = T | Record<string, any>;
type ModelConstructorSource<T> = T | Record<string, any> | string | number | undefined; // and any other types used by pks

export class ListQueryFilter implements ListFilter
{
  filter?: QueryFilter;
  sort?: string[];

  constructor( { filter, sort }: { filter?: QueryFilter, sort?: string[] } )
  {
    this.filter = filter;
    this.sort = sort;
  }
}

export interface ListParamaters
{
  filter?: ListFilter;
  position?: number;
  count?: number;
}

function filterConvert( filter_items: Record<string, any> ): object
{
  const item_list = [];

  for( const [ field, value ] of Object.entries( filter_items ) )
  {
    if( value === undefined )
      continue;

    item_list.push( { 'field': field, 'value': value, 'operation': '=' } );
  }

  while( item_list.length > 1 )
    item_list.push( { 'operation': 'and', 'left': item_list.pop(), 'right': item_list.pop() } );

  return item_list[0];
}

export class MCP
{
  private cinp: CInP;

  constructor( host: string, using_cookies: boolean=false )
  {
    this.cinp = new CInP( host );
    this.cinp.using_cookies = using_cookies;
  }

  async check(): Promise<boolean>
  {
    return this.cinp.describe( '/api/v1/' ).then( res => res.version === '0.11' );
  }

  setHeader( name: string, value: string ): void
  {
    return this.cinp.setHeader( name, value );
  }

  clearHeader( name: string ): void
  {
    return this.cinp.clearHeader( name );
  }

  setServerErrorHandler( handler: ( message: string, trace: string ) => void ): void
  {
    this.cinp.server_error_handler = handler;
  }

  raw( verb: string, uri: string, data: object, header_map: Record<string, string> ): Promise<unknown>
  {
    return this.cinp.raw( verb, uri, data, header_map );
  }
  // Namespace  at /api/v1/ version 0.11
/*

*/

  // Model config at /api/v1/config

  async _config_create( data: _config ): Promise<void>
  {
    const response = await this.cinp.create<_config>( "/api/v1/config", data._as_map( true ) );

    data._update( response.data );
  }

  __config_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async _config_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, _config>>
  {
    const [ filter_name, filter_value_map ] = this.__config_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<_config>( "/api/v1/config", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new _config( this, response[ key ] );
    }

    return response;
  }

  async _config_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this.__config_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/config", filter_name, filter_value_map, position, count );
  }

  async _config_call_getContractorInfo(): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/config(getContractorInfo)", {} );
    return ( _cinp_result_ as {} );
  }


  async _config_call_getPackratInfo(): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/config(getPackratInfo)", {} );
    return ( _cinp_result_ as {} );
  }


  async _config_call_getConfluenceInfo(): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/config(getConfluenceInfo)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model config (end)
  // Namespace Auth at /api/v1/Auth/ version 2.0
/*

*/

  // Model User at /api/v1/Auth/User

  async Auth_User_call_login( username: string, password: string ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Auth/User(login)", { "username": username, "password": password } );
    return ( _cinp_result_ as string );
  }


  async Auth_User_call_logout(): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Auth/User(logout)", {} );
    return;
  }


  async Auth_User_call_whoami(): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Auth/User(whoami)", {} );
    return ( _cinp_result_ as string );
  }


  async Auth_User_call_changePassword( password: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Auth/User(changePassword)", { "password": password } );
    return;
  }


  // Model User (end)
  // Namespace Auth (end)
  // Namespace Resource at /api/v1/Resource/ version 0.1
/*

*/

  // Model Site at /api/v1/Resource/Site

  async Resource_Site_get( id: string ): Promise<Resource_Site>
  {
    const response = await this.cinp.getOne<Resource_Site>( "/api/v1/Resource/Site:" + id + ":" );

    return new Resource_Site( this, response );
  }

  _Resource_Site_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_Site_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_Site>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Site_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_Site>( "/api/v1/Resource/Site", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_Site( this, response[ key ] );
    }

    return response;
  }

  async Resource_Site_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Site_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/Site", filter_name, filter_value_map, position, count );
  }

  // Model Site (end)
  // Model Resource at /api/v1/Resource/Resource

  async Resource_Resource_get( id: string ): Promise<Resource_Resource>
  {
    const response = await this.cinp.getOne<Resource_Resource>( "/api/v1/Resource/Resource:" + id + ":" );

    return new Resource_Resource( this, response );
  }

  _Resource_Resource_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_Resource_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_Resource>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Resource_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_Resource>( "/api/v1/Resource/Resource", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_Resource( this, response[ key ] );
    }

    return response;
  }

  async Resource_Resource_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Resource_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/Resource", filter_name, filter_value_map, position, count );
  }

  // Model Resource (end)
  // Model ResourceInstance at /api/v1/Resource/ResourceInstance

  async Resource_ResourceInstance_get( id: number ): Promise<Resource_ResourceInstance>
  {
    const response = await this.cinp.getOne<Resource_ResourceInstance>( "/api/v1/Resource/ResourceInstance:" + id + ":" );

    return new Resource_ResourceInstance( this, response );
  }

  _Resource_ResourceInstance_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_ResourceInstance_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_ResourceInstance>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_ResourceInstance_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_ResourceInstance>( "/api/v1/Resource/ResourceInstance", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_ResourceInstance( this, response[ key ] );
    }

    return response;
  }

  async Resource_ResourceInstance_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_ResourceInstance_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/ResourceInstance", filter_name, filter_value_map, position, count );
  }

  // Model ResourceInstance (end)
  // Model StaticResource at /api/v1/Resource/StaticResource

  async Resource_StaticResource_get( id: string ): Promise<Resource_StaticResource>
  {
    const response = await this.cinp.getOne<Resource_StaticResource>( "/api/v1/Resource/StaticResource:" + id + ":" );

    return new Resource_StaticResource( this, response );
  }

  _Resource_StaticResource_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_StaticResource_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_StaticResource>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_StaticResource_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_StaticResource>( "/api/v1/Resource/StaticResource", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_StaticResource( this, response[ key ] );
    }

    return response;
  }

  async Resource_StaticResource_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_StaticResource_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/StaticResource", filter_name, filter_value_map, position, count );
  }

  // Model StaticResource (end)
  // Model StaticResourceInstance at /api/v1/Resource/StaticResourceInstance

  async Resource_StaticResourceInstance_get( id: number ): Promise<Resource_StaticResourceInstance>
  {
    const response = await this.cinp.getOne<Resource_StaticResourceInstance>( "/api/v1/Resource/StaticResourceInstance:" + id + ":" );

    return new Resource_StaticResourceInstance( this, response );
  }

  _Resource_StaticResourceInstance_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_StaticResourceInstance_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_StaticResourceInstance>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_StaticResourceInstance_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_StaticResourceInstance>( "/api/v1/Resource/StaticResourceInstance", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_StaticResourceInstance( this, response[ key ] );
    }

    return response;
  }

  async Resource_StaticResourceInstance_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_StaticResourceInstance_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/StaticResourceInstance", filter_name, filter_value_map, position, count );
  }

  // Model StaticResourceInstance (end)
  // Model DynamicResource at /api/v1/Resource/DynamicResource

  async Resource_DynamicResource_get( id: string ): Promise<Resource_DynamicResource>
  {
    const response = await this.cinp.getOne<Resource_DynamicResource>( "/api/v1/Resource/DynamicResource:" + id + ":" );

    return new Resource_DynamicResource( this, response );
  }

  _Resource_DynamicResource_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_DynamicResource_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_DynamicResource>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResource_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_DynamicResource>( "/api/v1/Resource/DynamicResource", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_DynamicResource( this, response[ key ] );
    }

    return response;
  }

  async Resource_DynamicResource_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResource_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/DynamicResource", filter_name, filter_value_map, position, count );
  }

  // Model DynamicResource (end)
  // Model DynamicResourceSite at /api/v1/Resource/DynamicResourceSite

  async Resource_DynamicResourceSite_get( id: number ): Promise<Resource_DynamicResourceSite>
  {
    const response = await this.cinp.getOne<Resource_DynamicResourceSite>( "/api/v1/Resource/DynamicResourceSite:" + id + ":" );

    return new Resource_DynamicResourceSite( this, response );
  }

  async Resource_DynamicResourceSite_create( data: Resource_DynamicResourceSite ): Promise<void>
  {
    const response = await this.cinp.create<Resource_DynamicResourceSite>( "/api/v1/Resource/DynamicResourceSite", data._as_map( true ) );

    data._update( response.data );
  }

  async Resource_DynamicResourceSite_update( id: number, data: Resource_DynamicResourceSite ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Resource_DynamicResourceSite>( "/api/v1/Resource/DynamicResourceSite:" + id + ":", data._as_map( false ) ) );
  }

  async Resource_DynamicResourceSite_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Resource/DynamicResourceSite:" + id + ":" );
  }

  _Resource_DynamicResourceSite_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_DynamicResourceSite_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_DynamicResourceSite>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResourceSite_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_DynamicResourceSite>( "/api/v1/Resource/DynamicResourceSite", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_DynamicResourceSite( this, response[ key ] );
    }

    return response;
  }

  async Resource_DynamicResourceSite_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResourceSite_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/DynamicResourceSite", filter_name, filter_value_map, position, count );
  }

  // Model DynamicResourceSite (end)
  // Model DynamicResourceInstance at /api/v1/Resource/DynamicResourceInstance

  async Resource_DynamicResourceInstance_get( id: number ): Promise<Resource_DynamicResourceInstance>
  {
    const response = await this.cinp.getOne<Resource_DynamicResourceInstance>( "/api/v1/Resource/DynamicResourceInstance:" + id + ":" );

    return new Resource_DynamicResourceInstance( this, response );
  }

  _Resource_DynamicResourceInstance_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_DynamicResourceInstance_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_DynamicResourceInstance>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResourceInstance_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_DynamicResourceInstance>( "/api/v1/Resource/DynamicResourceInstance", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_DynamicResourceInstance( this, response[ key ] );
    }

    return response;
  }

  async Resource_DynamicResourceInstance_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_DynamicResourceInstance_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/DynamicResourceInstance", filter_name, filter_value_map, position, count );
  }

  // Model DynamicResourceInstance (end)
  // Model Network at /api/v1/Resource/Network

  async Resource_Network_get( id: string ): Promise<Resource_Network>
  {
    const response = await this.cinp.getOne<Resource_Network>( "/api/v1/Resource/Network:" + id + ":" );

    return new Resource_Network( this, response );
  }

  _Resource_Network_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Resource_Network_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Resource_Network>>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Network_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Resource_Network>( "/api/v1/Resource/Network", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Resource_Network( this, response[ key ] );
    }

    return response;
  }

  async Resource_Network_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Resource_Network_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Resource/Network", filter_name, filter_value_map, position, count );
  }

  // Model Network (end)
  // Namespace Resource (end)
  // Namespace Project at /api/v1/Project/ version 0.1
/*

*/

  // Model Project at /api/v1/Project/Project

  async Project_Project_get( id: string ): Promise<Project_Project>
  {
    const response = await this.cinp.getOne<Project_Project>( "/api/v1/Project/Project:" + id + ":" );

    return new Project_Project( this, response );
  }

  _Project_Project_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Project_Project._ListFilter_my_projects )
      return [ 'my_projects', {  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_Project_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_Project>>
  {
    const [ filter_name, filter_value_map ] = this._Project_Project_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_Project>( "/api/v1/Project/Project", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_Project( this, response[ key ] );
    }

    return response;
  }

  async Project_Project_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_Project_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/Project", filter_name, filter_value_map, position, count );
  }

  // Model Project (end)
  // Model GitProject at /api/v1/Project/GitProject

  async Project_GitProject_get( id: string ): Promise<Project_GitProject>
  {
    const response = await this.cinp.getOne<Project_GitProject>( "/api/v1/Project/GitProject:" + id + ":" );

    return new Project_GitProject( this, response );
  }

  async Project_GitProject_create( data: Project_GitProject ): Promise<void>
  {
    const response = await this.cinp.create<Project_GitProject>( "/api/v1/Project/GitProject", data._as_map( true ) );

    data._update( response.data );
  }

  async Project_GitProject_update( id: string, data: Project_GitProject ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Project_GitProject>( "/api/v1/Project/GitProject:" + id + ":", data._as_map( false ) ) );
  }

  async Project_GitProject_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Project/GitProject:" + id + ":" );
  }

  _Project_GitProject_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_GitProject_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_GitProject>>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitProject_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_GitProject>( "/api/v1/Project/GitProject", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_GitProject( this, response[ key ] );
    }

    return response;
  }

  async Project_GitProject_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitProject_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/GitProject", filter_name, filter_value_map, position, count );
  }

  // Model GitProject (end)
  // Model GitHubProject at /api/v1/Project/GitHubProject

  async Project_GitHubProject_get( id: string ): Promise<Project_GitHubProject>
  {
    const response = await this.cinp.getOne<Project_GitHubProject>( "/api/v1/Project/GitHubProject:" + id + ":" );

    return new Project_GitHubProject( this, response );
  }

  async Project_GitHubProject_create( data: Project_GitHubProject ): Promise<void>
  {
    const response = await this.cinp.create<Project_GitHubProject>( "/api/v1/Project/GitHubProject", data._as_map( true ) );

    data._update( response.data );
  }

  async Project_GitHubProject_update( id: string, data: Project_GitHubProject ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Project_GitHubProject>( "/api/v1/Project/GitHubProject:" + id + ":", data._as_map( false ) ) );
  }

  async Project_GitHubProject_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Project/GitHubProject:" + id + ":" );
  }

  _Project_GitHubProject_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_GitHubProject_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_GitHubProject>>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitHubProject_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_GitHubProject>( "/api/v1/Project/GitHubProject", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_GitHubProject( this, response[ key ] );
    }

    return response;
  }

  async Project_GitHubProject_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitHubProject_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/GitHubProject", filter_name, filter_value_map, position, count );
  }

  // Model GitHubProject (end)
  // Model GitLabProject at /api/v1/Project/GitLabProject

  async Project_GitLabProject_get( id: string ): Promise<Project_GitLabProject>
  {
    const response = await this.cinp.getOne<Project_GitLabProject>( "/api/v1/Project/GitLabProject:" + id + ":" );

    return new Project_GitLabProject( this, response );
  }

  async Project_GitLabProject_create( data: Project_GitLabProject ): Promise<void>
  {
    const response = await this.cinp.create<Project_GitLabProject>( "/api/v1/Project/GitLabProject", data._as_map( true ) );

    data._update( response.data );
  }

  async Project_GitLabProject_update( id: string, data: Project_GitLabProject ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Project_GitLabProject>( "/api/v1/Project/GitLabProject:" + id + ":", data._as_map( false ) ) );
  }

  async Project_GitLabProject_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Project/GitLabProject:" + id + ":" );
  }

  _Project_GitLabProject_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_GitLabProject_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_GitLabProject>>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitLabProject_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_GitLabProject>( "/api/v1/Project/GitLabProject", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_GitLabProject( this, response[ key ] );
    }

    return response;
  }

  async Project_GitLabProject_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_GitLabProject_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/GitLabProject", filter_name, filter_value_map, position, count );
  }

  // Model GitLabProject (end)
  // Model Package at /api/v1/Project/Package

  async Project_Package_get( id: string ): Promise<Project_Package>
  {
    const response = await this.cinp.getOne<Project_Package>( "/api/v1/Project/Package:" + id + ":" );

    return new Project_Package( this, response );
  }

  _Project_Package_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_Package_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_Package>>
  {
    const [ filter_name, filter_value_map ] = this._Project_Package_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_Package>( "/api/v1/Project/Package", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_Package( this, response[ key ] );
    }

    return response;
  }

  async Project_Package_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_Package_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/Package", filter_name, filter_value_map, position, count );
  }

  // Model Package (end)
  // Model Commit at /api/v1/Project/Commit

  async Project_Commit_get( id: number ): Promise<Project_Commit>
  {
    const response = await this.cinp.getOne<Project_Commit>( "/api/v1/Project/Commit:" + id + ":" );

    return new Project_Commit( this, response );
  }

  _Project_Commit_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Project_Commit._ListFilter_project )
      return [ 'project', { project: filter.project.toURL(),  } ];

    if( filter instanceof Project_Commit._ListFilter_in_process )
      return [ 'in_process', {  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_Commit_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_Commit>>
  {
    const [ filter_name, filter_value_map ] = this._Project_Commit_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_Commit>( "/api/v1/Project/Commit", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_Commit( this, response[ key ] );
    }

    return response;
  }

  async Project_Commit_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_Commit_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/Commit", filter_name, filter_value_map, position, count );
  }

  // Model Commit (end)
  // Model PackageFile at /api/v1/Project/PackageFile

  async Project_PackageFile_get( id: number ): Promise<Project_PackageFile>
  {
    const response = await this.cinp.getOne<Project_PackageFile>( "/api/v1/Project/PackageFile:" + id + ":" );

    return new Project_PackageFile( this, response );
  }

  _Project_PackageFile_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Project_PackageFile._ListFilter_project )
      return [ 'project', { package: filter.package.toURL(),  } ];

    if( filter instanceof Project_PackageFile._ListFilter_commit )
      return [ 'commit', { commit: filter.commit.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_PackageFile_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_PackageFile>>
  {
    const [ filter_name, filter_value_map ] = this._Project_PackageFile_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_PackageFile>( "/api/v1/Project/PackageFile", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_PackageFile( this, response[ key ] );
    }

    return response;
  }

  async Project_PackageFile_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_PackageFile_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/PackageFile", filter_name, filter_value_map, position, count );
  }

  // Model PackageFile (end)
  // Model Build at /api/v1/Project/Build

  async Project_Build_get( id: string ): Promise<Project_Build>
  {
    const response = await this.cinp.getOne<Project_Build>( "/api/v1/Project/Build:" + id + ":" );

    return new Project_Build( this, response );
  }

  _Project_Build_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Project_Build._ListFilter_project )
      return [ 'project', { project: filter.project.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_Build_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_Build>>
  {
    const [ filter_name, filter_value_map ] = this._Project_Build_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_Build>( "/api/v1/Project/Build", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_Build( this, response[ key ] );
    }

    return response;
  }

  async Project_Build_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_Build_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/Build", filter_name, filter_value_map, position, count );
  }

  // Model Build (end)
  // Model BuildDependancy at /api/v1/Project/BuildDependancy

  async Project_BuildDependancy_get( id: string ): Promise<Project_BuildDependancy>
  {
    const response = await this.cinp.getOne<Project_BuildDependancy>( "/api/v1/Project/BuildDependancy:" + id + ":" );

    return new Project_BuildDependancy( this, response );
  }

  _Project_BuildDependancy_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_BuildDependancy_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_BuildDependancy>>
  {
    const [ filter_name, filter_value_map ] = this._Project_BuildDependancy_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_BuildDependancy>( "/api/v1/Project/BuildDependancy", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_BuildDependancy( this, response[ key ] );
    }

    return response;
  }

  async Project_BuildDependancy_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_BuildDependancy_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/BuildDependancy", filter_name, filter_value_map, position, count );
  }

  // Model BuildDependancy (end)
  // Model BuildResource at /api/v1/Project/BuildResource

  async Project_BuildResource_get( id: string ): Promise<Project_BuildResource>
  {
    const response = await this.cinp.getOne<Project_BuildResource>( "/api/v1/Project/BuildResource:" + id + ":" );

    return new Project_BuildResource( this, response );
  }

  _Project_BuildResource_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Project_BuildResource_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Project_BuildResource>>
  {
    const [ filter_name, filter_value_map ] = this._Project_BuildResource_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Project_BuildResource>( "/api/v1/Project/BuildResource", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Project_BuildResource( this, response[ key ] );
    }

    return response;
  }

  async Project_BuildResource_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Project_BuildResource_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Project/BuildResource", filter_name, filter_value_map, position, count );
  }

  // Model BuildResource (end)
  // Namespace Project (end)
  // Namespace Processor at /api/v1/Processor/ version 0.1
/*

*/

  // Model Promotion at /api/v1/Processor/Promotion

  async Processor_Promotion_get( id: number ): Promise<Processor_Promotion>
  {
    const response = await this.cinp.getOne<Processor_Promotion>( "/api/v1/Processor/Promotion:" + id + ":" );

    return new Processor_Promotion( this, response );
  }

  _Processor_Promotion_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Processor_Promotion._ListFilter_in_process )
      return [ 'in_process', {  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Processor_Promotion_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Processor_Promotion>>
  {
    const [ filter_name, filter_value_map ] = this._Processor_Promotion_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Processor_Promotion>( "/api/v1/Processor/Promotion", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Processor_Promotion( this, response[ key ] );
    }

    return response;
  }

  async Processor_Promotion_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Processor_Promotion_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Processor/Promotion", filter_name, filter_value_map, position, count );
  }

  // Model Promotion (end)
  // Model PromotionBuild at /api/v1/Processor/PromotionBuild

  async Processor_PromotionBuild_get( id: number ): Promise<Processor_PromotionBuild>
  {
    const response = await this.cinp.getOne<Processor_PromotionBuild>( "/api/v1/Processor/PromotionBuild:" + id + ":" );

    return new Processor_PromotionBuild( this, response );
  }

  _Processor_PromotionBuild_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Processor_PromotionBuild_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Processor_PromotionBuild>>
  {
    const [ filter_name, filter_value_map ] = this._Processor_PromotionBuild_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Processor_PromotionBuild>( "/api/v1/Processor/PromotionBuild", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Processor_PromotionBuild( this, response[ key ] );
    }

    return response;
  }

  async Processor_PromotionBuild_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Processor_PromotionBuild_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Processor/PromotionBuild", filter_name, filter_value_map, position, count );
  }

  // Model PromotionBuild (end)
  // Model QueueItem at /api/v1/Processor/QueueItem

  async Processor_QueueItem_get( id: number ): Promise<Processor_QueueItem>
  {
    const response = await this.cinp.getOne<Processor_QueueItem>( "/api/v1/Processor/QueueItem:" + id + ":" );

    return new Processor_QueueItem( this, response );
  }

  _Processor_QueueItem_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Processor_QueueItem._ListFilter_project )
      return [ 'project', { project: filter.project.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Processor_QueueItem_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Processor_QueueItem>>
  {
    const [ filter_name, filter_value_map ] = this._Processor_QueueItem_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Processor_QueueItem>( "/api/v1/Processor/QueueItem", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Processor_QueueItem( this, response[ key ] );
    }

    return response;
  }

  async Processor_QueueItem_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Processor_QueueItem_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Processor/QueueItem", filter_name, filter_value_map, position, count );
  }

  async Processor_QueueItem_call_queue( build: Project_Build, branch: string, priority: number ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/QueueItem(queue)", { "build": build.toURL(), "branch": branch, "priority": priority } );
    return ( _cinp_result_ as number );
  }


  // Model QueueItem (end)
  // Model BuildJob at /api/v1/Processor/BuildJob

  async Processor_BuildJob_get( id: number ): Promise<Processor_BuildJob>
  {
    const response = await this.cinp.getOne<Processor_BuildJob>( "/api/v1/Processor/BuildJob:" + id + ":" );

    return new Processor_BuildJob( this, response );
  }

  _Processor_BuildJob_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Processor_BuildJob._ListFilter_project )
      return [ 'project', { project: filter.project.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Processor_BuildJob_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Processor_BuildJob>>
  {
    const [ filter_name, filter_value_map ] = this._Processor_BuildJob_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Processor_BuildJob>( "/api/v1/Processor/BuildJob", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Processor_BuildJob( this, response[ key ] );
    }

    return response;
  }

  async Processor_BuildJob_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Processor_BuildJob_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Processor/BuildJob", filter_name, filter_value_map, position, count );
  }

  async Processor_BuildJob_call_jobRan( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJob:" + id + ":(jobRan)", {} );
    return;
  }


  async Processor_BuildJob_call_acknowledge( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJob:" + id + ":(acknowledge)", {} );
    return;
  }


  async Processor_BuildJob_call_getInstanceState( id: number ,  name: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/BuildJob:" + id + ":(getInstanceState)", { "name": name } );
    return ( _cinp_result_ as {} );
  }


  async Processor_BuildJob_call_getInstanceStructureId( id: number ,  name: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/BuildJob:" + id + ":(getInstanceStructureId)", { "name": name } );
    return ( _cinp_result_ as {} );
  }


  // Model BuildJob (end)
  // Model BuildJobResourceInstance at /api/v1/Processor/BuildJobResourceInstance

  async Processor_BuildJobResourceInstance_get( id: number ): Promise<Processor_BuildJobResourceInstance>
  {
    const response = await this.cinp.getOne<Processor_BuildJobResourceInstance>( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":" );

    return new Processor_BuildJobResourceInstance( this, response );
  }

  _Processor_BuildJobResourceInstance_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Processor_BuildJobResourceInstance_get_multi( { filter, position, count }: ListParamaters ): Promise<Record<string, Processor_BuildJobResourceInstance>>
  {
    const [ filter_name, filter_value_map ] = this._Processor_BuildJobResourceInstance_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Processor_BuildJobResourceInstance>( "/api/v1/Processor/BuildJobResourceInstance", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Processor_BuildJobResourceInstance( this, response[ key ] );
    }

    return response;
  }

  async Processor_BuildJobResourceInstance_list( { filter, position, count }: ListParamaters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Processor_BuildJobResourceInstance_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Processor/BuildJobResourceInstance", filter_name, filter_value_map, position, count );
  }

  async Processor_BuildJobResourceInstance_call_signalBuilt( id: number ,  cookie: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(signalBuilt)", { "cookie": cookie } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_signalDestroyed( id: number ,  cookie: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(signalDestroyed)", { "cookie": cookie } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_setMessage( id: number ,  cookie: string, message: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(setMessage)", { "cookie": cookie, "message": message } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_jobRan( id: number ,  cookie: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(jobRan)", { "cookie": cookie } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_setSuccess( id: number ,  cookie: string, success: boolean ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(setSuccess)", { "cookie": cookie, "success": success } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_setResults( id: number ,  cookie: string, target: string, results: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(setResults)", { "cookie": cookie, "target": target, "results": results } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_setScore( id: number ,  cookie: string, target: string, score: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(setScore)", { "cookie": cookie, "target": target, "score": score } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_addPackageFiles( id: number ,  cookie: string, package_file_map: {} ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(addPackageFiles)", { "cookie": cookie, "package_file_map": package_file_map } );
    return ( _cinp_result_ as string );
  }


  async Processor_BuildJobResourceInstance_call_getValueMap( id: number ,  cookie: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(getValueMap)", { "cookie": cookie } );
    return ( _cinp_result_ as {} );
  }


  async Processor_BuildJobResourceInstance_call_updateValueMap( id: number ,  cookie: string, value_map: {} ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(updateValueMap)", { "cookie": cookie, "value_map": value_map } );
    return;
  }


  async Processor_BuildJobResourceInstance_call_getHostDetail( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Processor/BuildJobResourceInstance:" + id + ":(getHostDetail)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model BuildJobResourceInstance (end)
  // Namespace Processor (end)
  // Namespace  (end)
}

export default MCP;
// Model config from  at /api/v1/config
/*

*/
export class _config
{
  private _service: MCP;



  constructor( service: MCP, source?: ModelConstructorSource<_config> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as _config;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<_config> ): void
  {
    data = data as _config;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_getContractorInfo(  ): Promise<{}>
  {
    return this._service._config_call_getContractorInfo( );
  }


  async _call_getPackratInfo(  ): Promise<{}>
  {
    return this._service._config_call_getPackratInfo( );
  }


  async _call_getConfluenceInfo(  ): Promise<{}>
  {
    return this._service._config_call_getConfluenceInfo( );
  }


}

export namespace _config
{

}

// Model User from Auth at /api/v1/Auth/User
/*

*/
export class Auth_User
{
  private _service: MCP;



  constructor( service: MCP, source?: ModelConstructorSource<Auth_User> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Auth_User;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<Auth_User> ): void
  {
    data = data as Auth_User;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_login(  username: string, password: string  ): Promise<string>
  {
    return this._service.Auth_User_call_login( username, password  );
  }


  async _call_logout(  ): Promise<void>
  {
    return this._service.Auth_User_call_logout( );
  }


  async _call_whoami(  ): Promise<string>
  {
    return this._service.Auth_User_call_whoami( );
  }


  async _call_changePassword(  password: string  ): Promise<void>
  {
    return this._service.Auth_User_call_changePassword( password  );
  }


}

export namespace Auth_User
{

}

// Model Site from Resource at /api/v1/Resource/Site
/*
Site
*/
export class Resource_Site
{
  private _service: MCP;

  public name: string = '';

  public generic: boolean | undefined = true;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_Site> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_Site;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/Site' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/Site:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_Site> ): void
  {
    data = data as Resource_Site;
    this.generic = data.generic;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'generic': this.generic,  };

    return { 'generic': this.generic,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_Site_get( this.name );

    this._update( record );
  }

}

export namespace Resource_Site
{

}

// Model Resource from Resource at /api/v1/Resource/Resource
/*
Resource(name, description, created, updated)
*/
export class Resource_Resource
{
  private _service: MCP;

  public name: string = '';

  public description: string | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_Resource> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_Resource;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/Resource' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/Resource:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_Resource> ): void
  {
    data = data as Resource_Resource;
    this.description = data.description;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description,  };

    return { 'description': this.description,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_Resource_get( this.name );

    this._update( record );
  }

}

export namespace Resource_Resource
{

}

// Model ResourceInstance from Resource at /api/v1/Resource/ResourceInstance
/*
ResourceInstance(id, site, contractor_structure_id, created, updated)
*/
export class Resource_ResourceInstance
{
  private _service: MCP;

  public id: number = 0;

  public site: Resource_Site | undefined = undefined;
  public contractor_structure_id: number | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_ResourceInstance> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_ResourceInstance;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/ResourceInstance' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/ResourceInstance:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_ResourceInstance> ): void
  {
    data = data as Resource_ResourceInstance;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.contractor_structure_id = data.contractor_structure_id;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_ResourceInstance_get( this.id );

    this._update( record );
  }

}

export namespace Resource_ResourceInstance
{

}

// Model StaticResource from Resource at /api/v1/Resource/StaticResource
/*
StaticResource
*/
export class Resource_StaticResource
{
  private _service: MCP;

  public name: string = '';

  public description: string | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public site: Resource_Site | undefined = undefined;
  public group_name: string | undefined = undefined;
  public interface_map: {} | undefined = {};

  constructor( service: MCP, source?: ModelConstructorSource<Resource_StaticResource> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_StaticResource;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/StaticResource' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/StaticResource:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_StaticResource> ): void
  {
    data = data as Resource_StaticResource;
    this.description = data.description;
    this.created = data.created;
    this.updated = data.updated;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.group_name = data.group_name;
    this.interface_map = data.interface_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'group_name': this.group_name, 'interface_map': this.interface_map,  };

    return { 'description': this.description, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'group_name': this.group_name, 'interface_map': this.interface_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_StaticResource_get( this.name );

    this._update( record );
  }

}

export namespace Resource_StaticResource
{

}

// Model StaticResourceInstance from Resource at /api/v1/Resource/StaticResourceInstance
/*
StaticResourceInstance
*/
export class Resource_StaticResourceInstance
{
  private _service: MCP;

  public id: number = 0;

  public site: Resource_Site | undefined = undefined;
  public contractor_structure_id: number | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public static_resource: Resource_StaticResource | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_StaticResourceInstance> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_StaticResourceInstance;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/StaticResourceInstance' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/StaticResourceInstance:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_StaticResourceInstance> ): void
  {
    data = data as Resource_StaticResourceInstance;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.contractor_structure_id = data.contractor_structure_id;
    this.created = data.created;
    this.updated = data.updated;
    this.static_resource = data.static_resource !== null ? new Resource_StaticResource( this._service, data.static_resource ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id, 'static_resource': this.static_resource !== undefined ? this.static_resource.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id, 'static_resource': this.static_resource !== undefined ? this.static_resource.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_StaticResourceInstance_get( this.id );

    this._update( record );
  }

}

export namespace Resource_StaticResourceInstance
{

}

// Model DynamicResource from Resource at /api/v1/Resource/DynamicResource
/*
DynamicResource
*/
export class Resource_DynamicResource
{
  private _service: MCP;

  public name: string = '';

  public description: string | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public build_ahead_count_map: {} | undefined = {};
  public sites: Resource_Site[] | undefined = [];

  constructor( service: MCP, source?: ModelConstructorSource<Resource_DynamicResource> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_DynamicResource;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/DynamicResource' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/DynamicResource:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_DynamicResource> ): void
  {
    data = data as Resource_DynamicResource;
    this.description = data.description;
    this.created = data.created;
    this.updated = data.updated;
    this.build_ahead_count_map = data.build_ahead_count_map;
    if( data.sites !== undefined )
      this.sites = data.sites.map( ( uri ) => { return new Resource_Site( this._service, uri ) } );
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'build_ahead_count_map': this.build_ahead_count_map, 'sites': this.sites !== undefined ? this.sites.map( i => i.toURL() ) : undefined,  };

    return { 'description': this.description, 'build_ahead_count_map': this.build_ahead_count_map, 'sites': this.sites !== undefined ? this.sites.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_DynamicResource_get( this.name );

    this._update( record );
  }

}

export namespace Resource_DynamicResource
{

}

// Model DynamicResourceSite from Resource at /api/v1/Resource/DynamicResourceSite
/*
DynamicResourceSite(id, dynamicresource, site, complex_id, updated, created)
*/
export class Resource_DynamicResourceSite
{
  private _service: MCP;

  public id: number = 0;

  public dynamicresource: Resource_DynamicResource | undefined = undefined;
  public site: Resource_Site | undefined = undefined;
  public complex_id: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_DynamicResourceSite> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_DynamicResourceSite;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/DynamicResourceSite' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/DynamicResourceSite:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_DynamicResourceSite> ): void
  {
    data = data as Resource_DynamicResourceSite;
    this.dynamicresource = data.dynamicresource !== null ? new Resource_DynamicResource( this._service, data.dynamicresource ) : undefined;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.complex_id = data.complex_id;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'dynamicresource': this.dynamicresource !== undefined ? this.dynamicresource.toURL() : undefined, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'complex_id': this.complex_id,  };

    return { 'dynamicresource': this.dynamicresource !== undefined ? this.dynamicresource.toURL() : undefined, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'complex_id': this.complex_id,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_DynamicResourceSite_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Resource_DynamicResourceSite_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Resource_DynamicResourceSite_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Resource_DynamicResourceSite_delete( this.id );
  }


}

export namespace Resource_DynamicResourceSite
{

}

// Model DynamicResourceInstance from Resource at /api/v1/Resource/DynamicResourceInstance
/*
DynamicResourceInstance
*/
export class Resource_DynamicResourceInstance
{
  private _service: MCP;

  public id: number = 0;

  public site: Resource_Site | undefined = undefined;
  public contractor_structure_id: number | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public dynamic_resource: Resource_DynamicResource | undefined = undefined;
  public contractor_foundation_id: string | undefined = undefined;
  public interface_map: {} | undefined = {};

  constructor( service: MCP, source?: ModelConstructorSource<Resource_DynamicResourceInstance> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_DynamicResourceInstance;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/DynamicResourceInstance' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/DynamicResourceInstance:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_DynamicResourceInstance> ): void
  {
    data = data as Resource_DynamicResourceInstance;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.contractor_structure_id = data.contractor_structure_id;
    this.created = data.created;
    this.updated = data.updated;
    this.dynamic_resource = data.dynamic_resource !== null ? new Resource_DynamicResource( this._service, data.dynamic_resource ) : undefined;
    this.contractor_foundation_id = data.contractor_foundation_id;
    this.interface_map = data.interface_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id, 'dynamic_resource': this.dynamic_resource !== undefined ? this.dynamic_resource.toURL() : undefined, 'contractor_foundation_id': this.contractor_foundation_id, 'interface_map': this.interface_map,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_structure_id': this.contractor_structure_id, 'dynamic_resource': this.dynamic_resource !== undefined ? this.dynamic_resource.toURL() : undefined, 'contractor_foundation_id': this.contractor_foundation_id, 'interface_map': this.interface_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_DynamicResourceInstance_get( this.id );

    this._update( record );
  }


}

export namespace Resource_DynamicResourceInstance
{

}

// Model Network from Resource at /api/v1/Resource/Network
/*
Network, name is the name of the SubNet/AddressBlock.
*/
export class Resource_Network
{
  private _service: MCP;

  public name: string = '';

  public site: Resource_Site | undefined = undefined;
  public contractor_addressblock_id: number | undefined = undefined;
  public contractor_network_id: number | undefined = undefined;
  public monolithic: boolean | undefined = true;
  public size: number | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Resource_Network> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Resource_Network;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Resource/Network' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Resource/Network:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Resource_Network> ): void
  {
    data = data as Resource_Network;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.contractor_addressblock_id = data.contractor_addressblock_id;
    this.contractor_network_id = data.contractor_network_id;
    this.monolithic = data.monolithic;
    this.size = data.size;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_addressblock_id': this.contractor_addressblock_id, 'contractor_network_id': this.contractor_network_id, 'monolithic': this.monolithic, 'size': this.size,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'contractor_addressblock_id': this.contractor_addressblock_id, 'contractor_network_id': this.contractor_network_id, 'monolithic': this.monolithic, 'size': this.size,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Resource_Network_get( this.name );

    this._update( record );
  }

}

export namespace Resource_Network
{

}

// Model Project from Project at /api/v1/Project/Project
/*
This is a Generic Project
*/
export class Project_Project
{
  private _service: MCP;

  public name: string = '';

  public release_branch: string | undefined = 'master';
  public build_counter: number | undefined = 0;
  public last_checked: Date | undefined = new Date( "0001-01-01T00:00:00+00:00" );
  public max_commit_count: number | undefined = 50;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public type: string | undefined = undefined;
  public busy: boolean | undefined = undefined;
  public upstream_git_url: string | undefined = undefined;
  public internal_git_url: string | undefined = undefined;
  public status: {} | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_Project> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_Project;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/Project' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/Project:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Project_Project> ): void
  {
    data = data as Project_Project;
    this.release_branch = data.release_branch;
    this.build_counter = data.build_counter;
    this.last_checked = data.last_checked;
    this.max_commit_count = data.max_commit_count;
    this.created = data.created;
    this.updated = data.updated;
    this.type = data.type;
    this.busy = data.busy;
    this.upstream_git_url = data.upstream_git_url;
    this.internal_git_url = data.internal_git_url;
    this.status = data.status;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count,  };

    return { 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_Project_get( this.name );

    this._update( record );
  }

}

export namespace Project_Project
{

  export class _ListFilter_my_projects implements ListFilter
  {

  };


}

// Model GitProject from Project at /api/v1/Project/GitProject
/*
This is a Git Project
*/
export class Project_GitProject
{
  private _service: MCP;

  public name: string = '';

  public release_branch: string | undefined = 'master';
  public local_path: string | undefined = undefined;
  public build_counter: number | undefined = 0;
  public last_checked: Date | undefined = new Date( "0001-01-01T00:00:00+00:00" );
  public max_commit_count: number | undefined = 50;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public git_repo: string | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_GitProject> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_GitProject;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/GitProject' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/GitProject:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Project_GitProject> ): void
  {
    data = data as Project_GitProject;
    this.release_branch = data.release_branch;
    this.local_path = data.local_path;
    this.build_counter = data.build_counter;
    this.last_checked = data.last_checked;
    this.max_commit_count = data.max_commit_count;
    this.created = data.created;
    this.updated = data.updated;
    this.git_repo = data.git_repo;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'git_repo': this.git_repo,  };

    return { 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'git_repo': this.git_repo,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_GitProject_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Project_GitProject_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Project_GitProject_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Project_GitProject_delete( this.name );
  }

}

export namespace Project_GitProject
{

}

// Model GitHubProject from Project at /api/v1/Project/GitHubProject
/*
This is a GitHub Project
*/
export class Project_GitHubProject
{
  private _service: MCP;

  public name: string = '';

  public release_branch: string | undefined = 'master';
  public local_path: string | undefined = undefined;
  public build_counter: number | undefined = 0;
  public last_checked: Date | undefined = new Date( "0001-01-01T00:00:00+00:00" );
  public max_commit_count: number | undefined = 50;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public github_org: string | undefined = undefined;
  public github_repo: string | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_GitHubProject> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_GitHubProject;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/GitHubProject' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/GitHubProject:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Project_GitHubProject> ): void
  {
    data = data as Project_GitHubProject;
    this.release_branch = data.release_branch;
    this.local_path = data.local_path;
    this.build_counter = data.build_counter;
    this.last_checked = data.last_checked;
    this.max_commit_count = data.max_commit_count;
    this.created = data.created;
    this.updated = data.updated;
    this.github_org = data.github_org;
    this.github_repo = data.github_repo;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'github_org': this.github_org, 'github_repo': this.github_repo,  };

    return { 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'github_org': this.github_org, 'github_repo': this.github_repo,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_GitHubProject_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Project_GitHubProject_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Project_GitHubProject_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Project_GitHubProject_delete( this.name );
  }

}

export namespace Project_GitHubProject
{

}

// Model GitLabProject from Project at /api/v1/Project/GitLabProject
/*
This is a GitLab Project
*/
export class Project_GitLabProject
{
  private _service: MCP;

  public name: string = '';

  public release_branch: string | undefined = 'master';
  public local_path: string | undefined = undefined;
  public build_counter: number | undefined = 0;
  public last_checked: Date | undefined = new Date( "0001-01-01T00:00:00+00:00" );
  public max_commit_count: number | undefined = 50;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public gitlab_project_path: string | undefined = undefined;
  public gitlab_project_id: number | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_GitLabProject> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_GitLabProject;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/GitLabProject' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/GitLabProject:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Project_GitLabProject> ): void
  {
    data = data as Project_GitLabProject;
    this.release_branch = data.release_branch;
    this.local_path = data.local_path;
    this.build_counter = data.build_counter;
    this.last_checked = data.last_checked;
    this.max_commit_count = data.max_commit_count;
    this.created = data.created;
    this.updated = data.updated;
    this.gitlab_project_path = data.gitlab_project_path;
    this.gitlab_project_id = data.gitlab_project_id;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'gitlab_project_path': this.gitlab_project_path, 'gitlab_project_id': this.gitlab_project_id,  };

    return { 'release_branch': this.release_branch, 'max_commit_count': this.max_commit_count, 'gitlab_project_path': this.gitlab_project_path, 'gitlab_project_id': this.gitlab_project_id,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_GitLabProject_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Project_GitLabProject_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Project_GitLabProject_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Project_GitLabProject_delete( this.name );
  }

}

export namespace Project_GitLabProject
{

}

// Model Package from Project at /api/v1/Project/Package
/*
This is a Package
*/
export class Project_Package
{
  private _service: MCP;

  public name: string = '';

  public packrat_id: string | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_Package> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_Package;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/Package' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/Package:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Project_Package> ): void
  {
    data = data as Project_Package;
    this.packrat_id = data.packrat_id;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'packrat_id': this.packrat_id,  };

    return { 'packrat_id': this.packrat_id,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_Package_get( this.name );

    this._update( record );
  }

}

export namespace Project_Package
{

}

// Model Commit from Project at /api/v1/Project/Commit
/*
A Single Commit of a Project
*/
export class Project_Commit
{
  private _service: MCP;

  public id: number = 0;

  public project: Project_Project | undefined = undefined;
  public branch: string | undefined = undefined;
  public commit: string | undefined = undefined;
  public version: string | undefined = undefined;
  public build_name: string | undefined = undefined;
  public lint_results: {} | undefined = {};
  public test_results: {} | undefined = {};
  public build_results: {} | undefined = {};
  public doc_results: {} | undefined = {};
  public test_at: Date | undefined = undefined;
  public build_at: Date | undefined = undefined;
  public doc_at: Date | undefined = undefined;
  public done_at: Date | undefined = undefined;
  public package_file_map: {} | undefined = {};
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public state: 'new' | 'tested' | 'built' | 'doced' | 'done' | undefined = undefined;
  public summary: {} | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_Commit> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_Commit;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/Commit' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/Commit:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Project_Commit> ): void
  {
    data = data as Project_Commit;
    this.project = data.project !== null ? new Project_Project( this._service, data.project ) : undefined;
    this.branch = data.branch;
    this.commit = data.commit;
    this.version = data.version;
    this.build_name = data.build_name;
    this.lint_results = data.lint_results;
    this.test_results = data.test_results;
    this.build_results = data.build_results;
    this.doc_results = data.doc_results;
    this.test_at = data.test_at;
    this.build_at = data.build_at;
    this.doc_at = data.doc_at;
    this.done_at = data.done_at;
    this.package_file_map = data.package_file_map;
    this.created = data.created;
    this.updated = data.updated;
    this.state = data.state;
    this.summary = data.summary;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'project': this.project !== undefined ? this.project.toURL() : undefined, 'branch': this.branch, 'commit': this.commit, 'version': this.version, 'build_name': this.build_name, 'lint_results': this.lint_results, 'test_results': this.test_results, 'build_results': this.build_results, 'doc_results': this.doc_results, 'package_file_map': this.package_file_map,  };

    return { 'project': this.project !== undefined ? this.project.toURL() : undefined, 'branch': this.branch, 'commit': this.commit, 'version': this.version, 'build_name': this.build_name, 'lint_results': this.lint_results, 'test_results': this.test_results, 'build_results': this.build_results, 'doc_results': this.doc_results, 'package_file_map': this.package_file_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_Commit_get( this.id );

    this._update( record );
  }

}

export namespace Project_Commit
{

  export class _ListFilter_project implements ListFilter
  {
    public project: Project_Project;

    constructor( project: Project_Project )
    {
      this.project = project;
    }

  };


  export class _ListFilter_in_process implements ListFilter
  {

  };


}

// Model PackageFile from Project at /api/v1/Project/PackageFile
/*
PackageFile(id, filename, package, packrat_id, commit, created, updated)
*/
export class Project_PackageFile
{
  private _service: MCP;

  public id: number = 0;

  public filename: string | undefined = undefined;
  public package: Project_Package | undefined = undefined;
  public packrat_id: string | undefined = undefined;
  public commit: Project_Commit | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_PackageFile> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_PackageFile;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/PackageFile' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/PackageFile:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Project_PackageFile> ): void
  {
    data = data as Project_PackageFile;
    this.filename = data.filename;
    this.package = data.package !== null ? new Project_Package( this._service, data.package ) : undefined;
    this.packrat_id = data.packrat_id;
    this.commit = data.commit !== null ? new Project_Commit( this._service, data.commit ) : undefined;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'filename': this.filename, 'package': this.package !== undefined ? this.package.toURL() : undefined, 'packrat_id': this.packrat_id, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined,  };

    return { 'filename': this.filename, 'package': this.package !== undefined ? this.package.toURL() : undefined, 'packrat_id': this.packrat_id, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_PackageFile_get( this.id );

    this._update( record );
  }

}

export namespace Project_PackageFile
{

  export class _ListFilter_project implements ListFilter
  {
    public package: Project_Package;

    constructor( package_: Project_Package )
    {
      this.package = package_;
    }

  };


  export class _ListFilter_commit implements ListFilter
  {
    public commit: Project_Commit;

    constructor( commit: Project_Commit )
    {
      this.commit = commit;
    }

  };


}

// Model Build from Project at /api/v1/Project/Build
/*
This is a type of Build that can be done
*/
export class Project_Build
{
  private _service: MCP;

  public key: string = '';

  public name: string | undefined = undefined;
  public project: Project_Project | undefined = undefined;
  public network_map: {} | undefined = {};
  public manual: boolean | undefined = undefined;
  public site: Resource_Site | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public dependancies: Project_Package[] | undefined = [];
  public resources: Resource_Resource[] | undefined = [];

  constructor( service: MCP, source?: ModelConstructorSource<Project_Build> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_Build;
      this.key = source.key;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/Build' ) )
    {
      this.key = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.key = source;
    }
  }

  toString(): string
  {
    return this.key.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/Build:' + this.key.toString() + ':';
  }

  _update( data: ModelUpdate<Project_Build> ): void
  {
    data = data as Project_Build;
    this.name = data.name;
    this.project = data.project !== null ? new Project_Project( this._service, data.project ) : undefined;
    this.network_map = data.network_map;
    this.manual = data.manual;
    this.site = data.site !== null ? new Resource_Site( this._service, data.site ) : undefined;
    this.created = data.created;
    this.updated = data.updated;
    if( data.dependancies !== undefined )
      this.dependancies = data.dependancies.map( ( uri ) => { return new Project_Package( this._service, uri ) } );
    if( data.resources !== undefined )
      this.resources = data.resources.map( ( uri ) => { return new Resource_Resource( this._service, uri ) } );
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'project': this.project !== undefined ? this.project.toURL() : undefined, 'network_map': this.network_map, 'manual': this.manual, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'dependancies': this.dependancies !== undefined ? this.dependancies.map( i => i.toURL() ) : undefined, 'resources': this.resources !== undefined ? this.resources.map( i => i.toURL() ) : undefined,  };

    return { 'name': this.name, 'project': this.project !== undefined ? this.project.toURL() : undefined, 'network_map': this.network_map, 'manual': this.manual, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'dependancies': this.dependancies !== undefined ? this.dependancies.map( i => i.toURL() ) : undefined, 'resources': this.resources !== undefined ? this.resources.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_Build_get( this.key );

    this._update( record );
  }

}

export namespace Project_Build
{

  export class _ListFilter_project implements ListFilter
  {
    public project: Project_Project;

    constructor( project: Project_Project )
    {
      this.project = project;
    }

  };


}

// Model BuildDependancy from Project at /api/v1/Project/BuildDependancy
/*
BuildDependancy(key, build, package, tag)
*/
export class Project_BuildDependancy
{
  private _service: MCP;

  public key: string = '';

  public build: Project_Build | undefined = undefined;
  public package: Project_Package | undefined = undefined;
  public tag: string | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Project_BuildDependancy> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_BuildDependancy;
      this.key = source.key;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/BuildDependancy' ) )
    {
      this.key = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.key = source;
    }
  }

  toString(): string
  {
    return this.key.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/BuildDependancy:' + this.key.toString() + ':';
  }

  _update( data: ModelUpdate<Project_BuildDependancy> ): void
  {
    data = data as Project_BuildDependancy;
    this.build = data.build !== null ? new Project_Build( this._service, data.build ) : undefined;
    this.package = data.package !== null ? new Project_Package( this._service, data.package ) : undefined;
    this.tag = data.tag;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'build': this.build !== undefined ? this.build.toURL() : undefined, 'package': this.package !== undefined ? this.package.toURL() : undefined, 'tag': this.tag,  };

    return { 'build': this.build !== undefined ? this.build.toURL() : undefined, 'package': this.package !== undefined ? this.package.toURL() : undefined, 'tag': this.tag,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_BuildDependancy_get( this.key );

    this._update( record );
  }

}

export namespace Project_BuildDependancy
{

}

// Model BuildResource from Project at /api/v1/Project/BuildResource
/*
BuildResource(key, build, name, resource, blueprint, config_values, quantity, auto_run, auto_provision, interface_map)
*/
export class Project_BuildResource
{
  private _service: MCP;

  public key: string = '';

  public build: Project_Build | undefined = undefined;
  public name: string | undefined = undefined;
  public resource: Resource_Resource | undefined = undefined;
  public blueprint: string | undefined = undefined;
  public config_values: {} | undefined = {};
  public quantity: number | undefined = 1;
  public auto_run: boolean | undefined = false;
  public auto_provision: boolean | undefined = true;
  public interface_map: {} | undefined = {};

  constructor( service: MCP, source?: ModelConstructorSource<Project_BuildResource> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Project_BuildResource;
      this.key = source.key;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Project/BuildResource' ) )
    {
      this.key = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.key = source;
    }
  }

  toString(): string
  {
    return this.key.toString();
  }

  toURL(): string
  {
    return '/api/v1/Project/BuildResource:' + this.key.toString() + ':';
  }

  _update( data: ModelUpdate<Project_BuildResource> ): void
  {
    data = data as Project_BuildResource;
    this.build = data.build !== null ? new Project_Build( this._service, data.build ) : undefined;
    this.name = data.name;
    this.resource = data.resource !== null ? new Resource_Resource( this._service, data.resource ) : undefined;
    this.blueprint = data.blueprint;
    this.config_values = data.config_values;
    this.quantity = data.quantity;
    this.auto_run = data.auto_run;
    this.auto_provision = data.auto_provision;
    this.interface_map = data.interface_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'build': this.build !== undefined ? this.build.toURL() : undefined, 'name': this.name, 'resource': this.resource !== undefined ? this.resource.toURL() : undefined, 'blueprint': this.blueprint, 'config_values': this.config_values, 'quantity': this.quantity, 'auto_run': this.auto_run, 'auto_provision': this.auto_provision, 'interface_map': this.interface_map,  };

    return { 'build': this.build !== undefined ? this.build.toURL() : undefined, 'name': this.name, 'resource': this.resource !== undefined ? this.resource.toURL() : undefined, 'blueprint': this.blueprint, 'config_values': this.config_values, 'quantity': this.quantity, 'auto_run': this.auto_run, 'auto_provision': this.auto_provision, 'interface_map': this.interface_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Project_BuildResource_get( this.key );

    this._update( record );
  }

}

export namespace Project_BuildResource
{

}

// Model Promotion from Processor at /api/v1/Processor/Promotion
/*
Promotion(id, result_map, commit, tag, done_at, created, updated)
*/
export class Processor_Promotion
{
  private _service: MCP;

  public id: number = 0;

  public result_map: {} | undefined = {};
  public commit: Project_Commit | undefined = undefined;
  public tag: string | undefined = undefined;
  public done_at: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public status: Project_Build[] | undefined = [];

  constructor( service: MCP, source?: ModelConstructorSource<Processor_Promotion> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Processor_Promotion;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Processor/Promotion' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Processor/Promotion:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Processor_Promotion> ): void
  {
    data = data as Processor_Promotion;
    this.result_map = data.result_map;
    this.commit = data.commit !== null ? new Project_Commit( this._service, data.commit ) : undefined;
    this.tag = data.tag;
    this.done_at = data.done_at;
    this.created = data.created;
    this.updated = data.updated;
    if( data.status !== undefined )
      this.status = data.status.map( ( uri ) => { return new Project_Build( this._service, uri ) } );
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'result_map': this.result_map, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'tag': this.tag, 'done_at': this.done_at, 'status': this.status !== undefined ? this.status.map( i => i.toURL() ) : undefined,  };

    return { 'result_map': this.result_map, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'tag': this.tag, 'done_at': this.done_at, 'status': this.status !== undefined ? this.status.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Processor_Promotion_get( this.id );

    this._update( record );
  }

}

export namespace Processor_Promotion
{

  export class _ListFilter_in_process implements ListFilter
  {

  };


}

// Model PromotionBuild from Processor at /api/v1/Processor/PromotionBuild
/*
PromotionBuild(id, promotion, build, status, success)
*/
export class Processor_PromotionBuild
{
  private _service: MCP;

  public id: number = 0;

  public promotion: Processor_Promotion | undefined = undefined;
  public build: Project_Build | undefined = undefined;
  public status: string | undefined = undefined;
  public success: boolean | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Processor_PromotionBuild> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Processor_PromotionBuild;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Processor/PromotionBuild' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Processor/PromotionBuild:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Processor_PromotionBuild> ): void
  {
    data = data as Processor_PromotionBuild;
    this.promotion = data.promotion !== null ? new Processor_Promotion( this._service, data.promotion ) : undefined;
    this.build = data.build !== null ? new Project_Build( this._service, data.build ) : undefined;
    this.status = data.status;
    this.success = data.success;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined, 'build': this.build !== undefined ? this.build.toURL() : undefined, 'status': this.status, 'success': this.success,  };

    return { 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined, 'build': this.build !== undefined ? this.build.toURL() : undefined, 'status': this.status, 'success': this.success,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Processor_PromotionBuild_get( this.id );

    this._update( record );
  }

}

export namespace Processor_PromotionBuild
{

}

// Model QueueItem from Processor at /api/v1/Processor/QueueItem
/*
QueueItem
*/
export class Processor_QueueItem
{
  private _service: MCP;

  public id: number = 0;

  public build: Project_Build | undefined = undefined;
  public project: Project_Project | undefined = undefined;
  public branch: string | undefined = undefined;
  public target: string | undefined = undefined;
  public priority: number | undefined = 50;
  public manual: boolean | undefined = undefined;
  public user: string | undefined = undefined;
  public resource_status_map: {} | undefined = {};
  public commit: Project_Commit | undefined = undefined;
  public promotion: Processor_Promotion | undefined = undefined;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Processor_QueueItem> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Processor_QueueItem;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Processor/QueueItem' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Processor/QueueItem:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Processor_QueueItem> ): void
  {
    data = data as Processor_QueueItem;
    this.build = data.build !== null ? new Project_Build( this._service, data.build ) : undefined;
    this.project = data.project !== null ? new Project_Project( this._service, data.project ) : undefined;
    this.branch = data.branch;
    this.target = data.target;
    this.priority = data.priority;
    this.manual = data.manual;
    this.user = data.user;
    this.resource_status_map = data.resource_status_map;
    this.commit = data.commit !== null ? new Project_Commit( this._service, data.commit ) : undefined;
    this.promotion = data.promotion !== null ? new Processor_Promotion( this._service, data.promotion ) : undefined;
    this.created = data.created;
    this.updated = data.updated;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'branch': this.branch, 'target': this.target, 'priority': this.priority, 'manual': this.manual, 'user': this.user, 'resource_status_map': this.resource_status_map, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined,  };

    return { 'branch': this.branch, 'target': this.target, 'priority': this.priority, 'manual': this.manual, 'user': this.user, 'resource_status_map': this.resource_status_map, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Processor_QueueItem_get( this.id );

    this._update( record );
  }


  async _call_queue(  build: Project_Build, branch: string, priority: number  ): Promise<number>
  {
    return this._service.Processor_QueueItem_call_queue( build, branch, priority  );
  }


}

export namespace Processor_QueueItem
{

  export class _ListFilter_project implements ListFilter
  {
    public project: Project_Project;

    constructor( project: Project_Project )
    {
      this.project = project;
    }

  };


}

// Model BuildJob from Processor at /api/v1/Processor/BuildJob
/*
BuildJob
*/
export class Processor_BuildJob
{
  private _service: MCP;

  public id: number = 0;

  public build: Project_Build | undefined = undefined;
  public project: Project_Project | undefined = undefined;
  public branch: string | undefined = undefined;
  public target: string | undefined = undefined;
  public value_map: {} | undefined = {};
  public built_at: Date | undefined = undefined;
  public ran_at: Date | undefined = undefined;
  public reported_at: Date | undefined = undefined;
  public acknowledged_at: Date | undefined = undefined;
  public released_at: Date | undefined = undefined;
  public manual: boolean | undefined = undefined;
  public user: string | undefined = undefined;
  public commit: Project_Commit | undefined = undefined;
  public promotion: Processor_Promotion | undefined = undefined;
  public package_file_map: {} | undefined = {};
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public network_list: Resource_Network[] | undefined = [];
  public resources: Resource_ResourceInstance[] | undefined = [];
  public state: 'new' | 'build' | 'ran' | 'reported' | 'acknowledged' | 'released' | undefined = undefined;
  public succeeded: boolean | undefined = undefined;
  public instance_summary: {} | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Processor_BuildJob> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Processor_BuildJob;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Processor/BuildJob' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Processor/BuildJob:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Processor_BuildJob> ): void
  {
    data = data as Processor_BuildJob;
    this.build = data.build !== null ? new Project_Build( this._service, data.build ) : undefined;
    this.project = data.project !== null ? new Project_Project( this._service, data.project ) : undefined;
    this.branch = data.branch;
    this.target = data.target;
    this.value_map = data.value_map;
    this.built_at = data.built_at;
    this.ran_at = data.ran_at;
    this.reported_at = data.reported_at;
    this.acknowledged_at = data.acknowledged_at;
    this.released_at = data.released_at;
    this.manual = data.manual;
    this.user = data.user;
    this.commit = data.commit !== null ? new Project_Commit( this._service, data.commit ) : undefined;
    this.promotion = data.promotion !== null ? new Processor_Promotion( this._service, data.promotion ) : undefined;
    this.package_file_map = data.package_file_map;
    this.created = data.created;
    this.updated = data.updated;
    if( data.network_list !== undefined )
      this.network_list = data.network_list.map( ( uri ) => { return new Resource_Network( this._service, uri ) } );
    if( data.resources !== undefined )
      this.resources = data.resources.map( ( uri ) => { return new Resource_ResourceInstance( this._service, uri ) } );
    this.state = data.state;
    this.succeeded = data.succeeded;
    this.instance_summary = data.instance_summary;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'branch': this.branch, 'target': this.target, 'value_map': this.value_map, 'manual': this.manual, 'user': this.user, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined, 'package_file_map': this.package_file_map, 'network_list': this.network_list !== undefined ? this.network_list.map( i => i.toURL() ) : undefined, 'resources': this.resources !== undefined ? this.resources.map( i => i.toURL() ) : undefined,  };

    return { 'branch': this.branch, 'target': this.target, 'value_map': this.value_map, 'manual': this.manual, 'user': this.user, 'commit': this.commit !== undefined ? this.commit.toURL() : undefined, 'promotion': this.promotion !== undefined ? this.promotion.toURL() : undefined, 'package_file_map': this.package_file_map, 'network_list': this.network_list !== undefined ? this.network_list.map( i => i.toURL() ) : undefined, 'resources': this.resources !== undefined ? this.resources.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Processor_BuildJob_get( this.id );

    this._update( record );
  }


  async _call_jobRan(  ): Promise<void>
  {
    return this._service.Processor_BuildJob_call_jobRan( this.id );
  }


  async _call_acknowledge(  ): Promise<void>
  {
    return this._service.Processor_BuildJob_call_acknowledge( this.id );
  }


  async _call_getInstanceState(  name: string  ): Promise<{}>
  {
    return this._service.Processor_BuildJob_call_getInstanceState( this.id,  name  );
  }


  async _call_getInstanceStructureId(  name: string  ): Promise<{}>
  {
    return this._service.Processor_BuildJob_call_getInstanceStructureId( this.id,  name  );
  }


}

export namespace Processor_BuildJob
{

  export class _ListFilter_project implements ListFilter
  {
    public project: Project_Project;

    constructor( project: Project_Project )
    {
      this.project = project;
    }

  };


}

// Model BuildJobResourceInstance from Processor at /api/v1/Processor/BuildJobResourceInstance
/*
BuildJobResourceInstance(id, buildjob, resource_instance, blueprint, _config_values, auto_run, auto_provision, cookie, name, index, state, message, success, created, updated)
*/
export class Processor_BuildJobResourceInstance
{
  private _service: MCP;

  public id: number = 0;

  public buildjob: Processor_BuildJob | undefined = undefined;
  public resource_instance: Resource_ResourceInstance | undefined = undefined;
  public blueprint: string | undefined = undefined;
  public _config_values: {} | undefined = {};
  public auto_run: boolean | undefined = false;
  public auto_provision: boolean | undefined = true;
  public cookie: string | undefined = '934c7026-2a55-4245-8649-767b1e1decd9';
  public name: string | undefined = undefined;
  public index: number | undefined = undefined;
  public state: 'new' | 'allocated' | 'building' | 'built' | 'ran' | 'releasing' | 'released' | undefined = 'new';
  public message: string | undefined = '';
  public success: boolean | undefined = false;
  public created: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public config_values: string | undefined = undefined;
  public hostname: string | undefined = undefined;

  constructor( service: MCP, source?: ModelConstructorSource<Processor_BuildJobResourceInstance> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Processor_BuildJobResourceInstance;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Processor/BuildJobResourceInstance' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Processor/BuildJobResourceInstance:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Processor_BuildJobResourceInstance> ): void
  {
    data = data as Processor_BuildJobResourceInstance;
    this.buildjob = data.buildjob !== null ? new Processor_BuildJob( this._service, data.buildjob ) : undefined;
    this.resource_instance = data.resource_instance !== null ? new Resource_ResourceInstance( this._service, data.resource_instance ) : undefined;
    this.blueprint = data.blueprint;
    this._config_values = data._config_values;
    this.auto_run = data.auto_run;
    this.auto_provision = data.auto_provision;
    this.cookie = data.cookie;
    this.name = data.name;
    this.index = data.index;
    this.state = data.state;
    this.message = data.message;
    this.success = data.success;
    this.created = data.created;
    this.updated = data.updated;
    this.config_values = data.config_values;
    this.hostname = data.hostname;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'buildjob': this.buildjob !== undefined ? this.buildjob.toURL() : undefined, 'resource_instance': this.resource_instance !== undefined ? this.resource_instance.toURL() : undefined, 'blueprint': this.blueprint, '_config_values': this._config_values, 'auto_run': this.auto_run, 'auto_provision': this.auto_provision, 'cookie': this.cookie, 'name': this.name, 'index': this.index, 'state': this.state, 'message': this.message, 'success': this.success,  };

    return { 'buildjob': this.buildjob !== undefined ? this.buildjob.toURL() : undefined, 'resource_instance': this.resource_instance !== undefined ? this.resource_instance.toURL() : undefined, 'blueprint': this.blueprint, '_config_values': this._config_values, 'auto_run': this.auto_run, 'auto_provision': this.auto_provision, 'cookie': this.cookie, 'name': this.name, 'index': this.index, 'state': this.state, 'message': this.message, 'success': this.success,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Processor_BuildJobResourceInstance_get( this.id );

    this._update( record );
  }


  async _call_signalBuilt(  cookie: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_signalBuilt( this.id,  cookie  );
  }


  async _call_signalDestroyed(  cookie: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_signalDestroyed( this.id,  cookie  );
  }


  async _call_setMessage(  cookie: string, message: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_setMessage( this.id,  cookie, message  );
  }


  async _call_jobRan(  cookie: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_jobRan( this.id,  cookie  );
  }


  async _call_setSuccess(  cookie: string, success: boolean  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_setSuccess( this.id,  cookie, success  );
  }


  async _call_setResults(  cookie: string, target: string, results: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_setResults( this.id,  cookie, target, results  );
  }


  async _call_setScore(  cookie: string, target: string, score: string  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_setScore( this.id,  cookie, target, score  );
  }


  async _call_addPackageFiles(  cookie: string, package_file_map: {}  ): Promise<string>
  {
    return this._service.Processor_BuildJobResourceInstance_call_addPackageFiles( this.id,  cookie, package_file_map  );
  }


  async _call_getValueMap(  cookie: string  ): Promise<{}>
  {
    return this._service.Processor_BuildJobResourceInstance_call_getValueMap( this.id,  cookie  );
  }


  async _call_updateValueMap(  cookie: string, value_map: {}  ): Promise<void>
  {
    return this._service.Processor_BuildJobResourceInstance_call_updateValueMap( this.id,  cookie, value_map  );
  }


  async _call_getHostDetail(  ): Promise<{}>
  {
    return this._service.Processor_BuildJobResourceInstance_call_getHostDetail( this.id );
  }


}

export namespace Processor_BuildJobResourceInstance
{

}
